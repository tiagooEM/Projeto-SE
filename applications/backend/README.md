# Backend (placeholder)

Este diretório deve conter o backend que atua como MQTT subscriber e fornece API para o frontend.

Sugestões de implementação:
- Python + Flask + paho-mqtt
- Node.js + Express + mqtt

Tarefas mínimas para checkpoint:
- Implementar subscriber que consome `casa/sala/corrente/rms` e loga os dados.
- Expor endpoint HTTP/WS para o frontend obter leituras em tempo real.

(Preencha aqui com instruções de execução e dependências quando implementar.)